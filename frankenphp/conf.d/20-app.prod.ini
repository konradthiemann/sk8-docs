; Production: the container is immutable, so opcache never needs to re-check files.
opcache.preload_user = root
opcache.preload = /app/config/preload.php
opcache.validate_timestamps = 0
memory_limit = 256M
