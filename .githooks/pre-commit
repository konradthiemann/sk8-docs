#!/usr/bin/env bash
# Pre-commit hook: static analysis, code style and tests (ADR-007).
# Activate once with scripts/setup.sh (sets core.hooksPath = .githooks).
set -euo pipefail

cd "$(git rev-parse --show-toplevel)"
exec make check
